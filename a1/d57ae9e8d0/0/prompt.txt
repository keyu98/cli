let's create a draft pr

---

/github-pr-review

---

can we run one of the e2e tests with claude and check the postTool file tracker output?

---

where do we wire in the fallback? or was that already there?

---

show me in the ide?

---

let's cut a new branch and start doing the other agents

---

I think we should do a PR per agent.

let's start with gemini, rename this branch appropriately.

but before we start, write a doc that details:
- what we need
- our research to date
- the wiring points and what we need to do to test the agent works

so we can refer to it when doing each PR

---

let's continue with gemini

---

check an e2e. we've got logging on the transcript fallback yes?

---

can we try a different e2e test? keep the repo around again

---

and we confirm that we didn't do a transcript read?

---

this is a stacked PR yes? on refactoring-transcript-reads?

---

/github-pr-review

---

we do need some mechanism to auto-update the hooks though - let's make a note of that in docs/plans for now and come back to it.

let's handle thread 3  - is thread 4 related to this PR specifically or should it be handled in the up-stack?